namespace :roles do
  desc "Create roles and default role"
  task :create_roles => :environment do
	# make default roles 

  end

  desc "Remake the permissions for the role"
  task :rebuild_permissions => :environment do
	

  end
end
