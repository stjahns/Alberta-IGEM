<h1>New note</h1>

<% form_for([@step, @note] ) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :note %><br/>
    <%= f.text_area :text %>
  </p>
  <p>
    <%= f.submit 'Add note' %>
  </p>
<% end %>

<%= link_to 'Back', :back %>
