<h2>New Construct</h1>

<% form_for :construct, :url => {:action => :create} do |f| %>
  <p>Name: <%= f.text_field :name %></p>
  <p><%= submit_tag 'Create' %></p>
<% end %>

<%= link_to 'Back', {:action => :index} %> 
